version: "3.7"

services:
  vscode:
    image: "docker.pkg.github.com/nolte/vscode-devcontainers/devops:latest"
    volumes:
      - "../:/go/src/github.com/nolte/tf-modules-hetzner:cached"
      - ~/.ssh:/home/vscode/.ssh:ro
      - /var/run/docker.sock:/var/run/docker.sock
      ## reuse volumes for extensions
      - tf-modules-hetzner-vol:/home/vscode/.vscode-server/extensions
      # And/or for VS Code Insiders
      - tf-modules-hetzner-vol-insiders:/home/vscode/.vscode-server-insiders/extension
    environment:
      - TZ=Europe/Berlin # $(cat /etc/timezone)
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined
    entrypoint: zsh -c "while sleep 1000; do :; done"
    network_mode: "host"

volumes:
  tf-modules-hetzner-vol:
  tf-modules-hetzner-vol-insiders:
